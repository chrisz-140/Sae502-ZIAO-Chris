- name: Port Scan with Nmap
  hosts: kali
  tasks:
    - name: Run port scan on target
      shell: nmap -sV {{ target_ip }}
      register: scan_results

    - name: Display scan results
      debug:
        var: scan_results.stdout
